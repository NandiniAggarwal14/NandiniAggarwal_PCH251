#include<stdio.h>
int main()
{
char str[40];
int i=0;
printf("\nEnter a string: ");
fgets(str,40,stdin);
while(str[i]!='\0')
{
if(str[i]>=65 && str[i]<=90)
{
str[i]=str[i]+32;
}
i++;
}
printf("\nThe new string is: ");
puts(str);
return 0;
}
